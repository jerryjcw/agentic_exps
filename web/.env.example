# Environment configuration for the web UI
# Copy this file to .env and adjust paths as needed

# Project root directory (auto-detected if not set)
# PROJECT_ROOT=/path/to/your/agentic_exps

# Upload directory within the web folder (auto-detected if not set)  
# UPLOAD_DIR=/path/to/your/agentic_exps/web/uploaded_files

# Development settings
NODE_ENV=development
VITE_DEV_MODE=true

# API endpoints (for future use)
# VITE_API_BASE_URL=http://localhost:8000
# VITE_FILE_UPLOAD_ENDPOINT=/api/files/upload